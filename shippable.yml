language: node_js

services:
  - postgres
  
node_js:
  - 8.9.0
  
build:
  ci:
    - psql -c 'create role shippable with superuser;' -U postgres
    - psql -c 'create database testdb;' -U postgres
    - npm install
    - npm test
    
